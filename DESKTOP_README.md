# 🤖 Jarvis AI Desktop Application

**Ускоренная десктопная версия с быстрым локальным распознаванием речи**

## ✨ Новые возможности

- **🚀 Быстрое распознавание речи** - локальные модели без задержек интернета
- **🖥️ Настоящее десктопное приложение** - работает без браузера
- **⚡ Гибридная система** - сначала локально, потом OpenAI как запасной вариант
- **🔧 Автоматический запуск** - backend запускается вместе с приложением

## 🛠️ Установка и запуск

### Быстрый старт

```bash
# 1. Установка всех зависимостей
./install-desktop.sh

# 2. Запуск приложения
./start-desktop.sh
```

### Ручная установка

```bash
# Установка Python зависимостей
pip3 install -r requirements.txt

# Установка Node.js зависимостей
npm install
cd frontend && npm install && cd ..

# Сборка frontend
cd frontend && npm run build && cd ..

# Запуск приложения
npm run dev
```

## 📁 Структура проекта

```
Jarvis_AI/
├── 📂 electron/              # Electron конфигурация
│   ├── main.js               # Главный процесс Electron
│   └── assets/               # Иконки и ресурсы
├── 📂 backend/               # Python FastAPI backend
│   ├── services/
│   │   ├── fast_speech_recognition.py  # Быстрое локальное распознавание
│   │   └── speech_recognition.py       # Основной распознаватель
│   └── main.py               # FastAPI приложение
├── 📂 frontend/              # React TypeScript frontend
├── install-desktop.sh        # Скрипт установки
├── start-desktop.sh          # Скрипт запуска
└── package.json              # Electron конфигурация
```

## 🚀 Технические улучшения

### Быстрое распознавание речи

- **Локальные модели**: Vosk, CMU Sphinx для офлайн распознавания
- **Fallback система**: автоматический переход к OpenAI Whisper
- **Оптимизированная обработка**: специальные настройки для речи

### Десктопная оптимизация

- **Прямой доступ к аудио**: системный микрофон без браузерных ограничений
- **Автоматический запуск backend**: Python сервер запускается с приложением
- **Улучшенная производительность**: оптимизированные аудио настройки

### Исправленные ошибки

- ✅ **async/await блокировки** - исправлены синхронные вызовы OpenAI API
- ✅ **Event loop проблемы** - добавлен thread pool для тяжелых операций
- ✅ **Audio processing ошибки** - улучшена обработка аудио данных

## 🎯 Быстрый запуск

### Development режим
```bash
npm run dev
```

### Production сборка
```bash
npm run dist
```

### Только frontend (веб-версия)
```bash
cd frontend && npm run dev
```

## 🔧 Конфигурация

### Переменные окружения (.env)
```env
# OpenAI API (для GPT-4 и Whisper)
OPENAI_API_KEY=your_openai_api_key

# ElevenLabs API (для синтеза речи)
ELEVENLABS_API_KEY=your_elevenlabs_api_key
ELEVENLABS_VOICE_ID=nPczCjzI2devNBz1zQrb

# Опционально: OpenWeatherMap для погоды
OPENWEATHERMAP_API_KEY=your_weather_api_key

# Настройки языка (ru/en)
LANGUAGE=ru
```

## 🏃‍♂️ Производительность

### Время отклика (цель)
- **Локальное распознавание**: < 500ms
- **OpenAI fallback**: 2-5 секунд
- **Общий отклик**: < 3 секунд

### Системные требования
- **Python**: 3.8+
- **Node.js**: 16+
- **RAM**: минимум 2GB
- **Микрофон**: обязателен

## 🔍 Устранение неполадок

### Проблемы с распознаванием речи
```bash
# Проверка микрофона
python3 -c "
import speech_recognition as sr
r = sr.Recognizer()
with sr.Microphone() as source:
    print('Микрофон найден:', source)
"

# Установка дополнительных пакетов для речи
pip3 install SpeechRecognition pyaudio pocketsphinx
```

### Проблемы с Electron
```bash
# Переустановка зависимостей
rm -rf node_modules
npm install

# Очистка кэша
npm run clean
```

## 📋 Список команд

Поддерживаемые голосовые команды:
- "Привет Джарвис" / "Hello Jarvis"
- "Какое время" / "What time"
- "Включить свет" / "Turn on light"
- "Выключить свет" / "Turn off light"
- "Какая погода" / "What's the weather"
- "Напомни..." / "Remind me..."
- "Выключись" / "Stop" / "Shutdown"

## 🆕 Новые возможности Desktop версии

### Улучшенный аудио процессор
- Автоматическая настройка микрофона
- Оптимизированные параметры записи
- Прямой доступ к системному аудио

### Статус приложения
- Отображение состояния backend
- Индикация подключения
- Мониторинг производительности

### Нативный интерфейс
- Полноэкранный режим
- Системное меню
- Горячие клавиши

---

## 📞 Поддержка

Если возникли проблемы:
1. Проверьте переменные окружения
2. Убедитесь в наличии API ключей
3. Проверьте подключение к интернету (для OpenAI)
4. Перезапустите приложение

**Версия**: 1.0.0 Desktop Edition  
**Автор**: Jarvis AI Team  
**Лицензия**: MIT