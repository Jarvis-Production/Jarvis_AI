# 📖 Полное руководство по установке Jarvis AI

Пошаговая инструкция для установки и запуска Jarvis AI на разных операционных системах.

## 📋 Содержание

1. [Требования к системе](#требования-к-системе)
2. [Установка на Windows](#установка-на-windows)
3. [Установка на macOS](#установка-на-macos)
4. [Установка на Linux](#установка-на-linux)
5. [Установка через Docker](#установка-через-docker)
6. [Получение API ключей](#получение-api-ключей)
7. [Проверка установки](#проверка-установки)
8. [Решение проблем](#решение-проблем)

---

## Требования к системе

### Минимальные требования
- **CPU**: 2 ядра, 2 GHz+
- **RAM**: 4 GB
- **Диск**: 2 GB свободного места
- **Интернет**: Стабильное соединение (для API запросов)

### Рекомендуемые требования
- **CPU**: 4 ядра, 3 GHz+
- **RAM**: 8 GB+
- **Диск**: 5 GB свободного места
- **Интернет**: Широкополосное соединение

### Программное обеспечение
- **Python**: 3.9 или выше
- **Node.js**: 16 или выше
- **npm**: 8 или выше (обычно идёт с Node.js)
- **Git**: Для клонирования репозитория
- **Браузер**: Chrome, Firefox, Edge или Safari (последние версии)

---

## Установка на Windows

### Шаг 1: Установка Python

1. Скачайте Python с https://www.python.org/downloads/
2. Запустите установщик
3. ✅ **Важно**: Отметьте "Add Python to PATH"
4. Нажмите "Install Now"
5. Проверьте установку:
   ```cmd
   python --version
   ```
   Должно показать: `Python 3.9.x` или выше

### Шаг 2: Установка Node.js

1. Скачайте Node.js с https://nodejs.org/
2. Запустите установщик (выберите LTS версию)
3. Следуйте инструкциям установщика
4. Проверьте установку:
   ```cmd
   node --version
   npm --version
   ```

### Шаг 3: Клонирование репозитория

```cmd
git clone https://github.com/yourusername/Jarvis_AI.git
cd Jarvis_AI
```

Или скачайте ZIP архив и распакуйте его.

### Шаг 4: Установка зависимостей Backend

```cmd
python -m pip install --upgrade pip
pip install -r requirements.txt
```

Если возникают проблемы с numpy:
```cmd
pip install numpy --only-binary :all:
```

### Шаг 5: Установка зависимостей Frontend

```cmd
cd frontend
npm install
cd ..
```

### Шаг 6: Настройка переменных окружения

1. Скопируйте файл `.env.example`:
   ```cmd
   copy .env.example .env
   ```

2. Откройте `.env` в текстовом редакторе (Notepad++)
3. Заполните API ключи (см. раздел "Получение API ключей")

### Шаг 7: Запуск

**Вариант 1: Автоматический запуск**
```cmd
start.bat
```

**Вариант 2: Ручной запуск**

Терминал 1 - Backend:
```cmd
python -m backend.main
```

Терминал 2 - Frontend:
```cmd
cd frontend
npm run dev
```

---

## Установка на macOS

### Шаг 1: Установка Homebrew (если не установлен)

```bash
/bin/bash -c "$(curl -fsSL https://raw.githubusercontent.com/Homebrew/install/HEAD/install.sh)"
```

### Шаг 2: Установка Python

```bash
brew install python@3.11
```

Проверка:
```bash
python3 --version
```

### Шаг 3: Установка Node.js

```bash
brew install node
```

Проверка:
```bash
node --version
npm --version
```

### Шаг 4: Клонирование репозитория

```bash
git clone https://github.com/yourusername/Jarvis_AI.git
cd Jarvis_AI
```

### Шаг 5: Создание виртуального окружения

```bash
python3 -m venv venv
source venv/bin/activate
```

### Шаг 6: Установка зависимостей Backend

```bash
pip install --upgrade pip
pip install -r requirements.txt
```

### Шаг 7: Установка зависимостей Frontend

```bash
cd frontend
npm install
cd ..
```

### Шаг 8: Настройка переменных окружения

```bash
cp .env.example .env
nano .env  # или используйте любой текстовый редактор
```

Заполните API ключи.

### Шаг 9: Запуск

**Вариант 1: Автоматический запуск**
```bash
chmod +x start.sh
./start.sh
```

**Вариант 2: Ручной запуск**

Терминал 1 - Backend:
```bash
source venv/bin/activate
python -m backend.main
```

Терминал 2 - Frontend:
```bash
cd frontend
npm run dev
```

---

## Установка на Linux

### Шаг 1: Обновление системы

**Ubuntu/Debian:**
```bash
sudo apt update && sudo apt upgrade -y
```

**Fedora:**
```bash
sudo dnf update -y
```

**Arch:**
```bash
sudo pacman -Syu
```

### Шаг 2: Установка Python

**Ubuntu/Debian:**
```bash
sudo apt install python3 python3-pip python3-venv -y
```

**Fedora:**
```bash
sudo dnf install python3 python3-pip -y
```

**Arch:**
```bash
sudo pacman -S python python-pip
```

Проверка:
```bash
python3 --version
```

### Шаг 3: Установка Node.js

**Ubuntu/Debian:**
```bash
curl -fsSL https://deb.nodesource.com/setup_18.x | sudo -E bash -
sudo apt install nodejs -y
```

**Fedora:**
```bash
sudo dnf install nodejs npm -y
```

**Arch:**
```bash
sudo pacman -S nodejs npm
```

Проверка:
```bash
node --version
npm --version
```

### Шаг 4: Клонирование репозитория

```bash
git clone https://github.com/yourusername/Jarvis_AI.git
cd Jarvis_AI
```

### Шаг 5: Создание виртуального окружения

```bash
python3 -m venv venv
source venv/bin/activate
```

### Шаг 6: Установка зависимостей Backend

```bash
pip install --upgrade pip
pip install -r requirements.txt
```

### Шаг 7: Установка зависимостей Frontend

```bash
cd frontend
npm install
cd ..
```

### Шаг 8: Настройка переменных окружения

```bash
cp .env.example .env
nano .env  # или vim, gedit и т.д.
```

Заполните API ключи.

### Шаг 9: Запуск

**Вариант 1: Автоматический запуск**
```bash
chmod +x start.sh
./start.sh
```

**Вариант 2: Ручной запуск**

Терминал 1 - Backend:
```bash
source venv/bin/activate
python -m backend.main
```

Терминал 2 - Frontend:
```bash
cd frontend
npm run dev
```

---

## Установка через Docker

### Требования
- Docker 20.10+
- Docker Compose 2.0+

### Шаг 1: Установка Docker

**Windows:**
- Скачайте Docker Desktop: https://www.docker.com/products/docker-desktop

**macOS:**
```bash
brew install --cask docker
```

**Linux (Ubuntu):**
```bash
curl -fsSL https://get.docker.com -o get-docker.sh
sudo sh get-docker.sh
sudo usermod -aG docker $USER
```

### Шаг 2: Клонирование и настройка

```bash
git clone https://github.com/yourusername/Jarvis_AI.git
cd Jarvis_AI
cp .env.example .env
# Отредактируйте .env файл
```

### Шаг 3: Запуск

```bash
docker-compose up --build
```

Для запуска в фоне:
```bash
docker-compose up -d
```

Остановка:
```bash
docker-compose down
```

---

## Получение API ключей

### OpenAI API Key (обязательно)

1. Перейдите на https://platform.openai.com/
2. Зарегистрируйтесь или войдите
3. Перейдите в "API Keys": https://platform.openai.com/api-keys
4. Нажмите "Create new secret key"
5. Скопируйте ключ (он начинается с `sk-`)
6. Пополните баланс: https://platform.openai.com/account/billing
   - Минимум $5-10 для тестирования
   - Whisper: ~$0.006/минута аудио
   - GPT-4: ~$0.03-0.06 за 1000 токенов

### ElevenLabs API Key (обязательно)

1. Перейдите на https://elevenlabs.io/
2. Зарегистрируйтесь
3. Перейдите в Profile → API Key
4. Скопируйте ваш API ключ
5. Бесплатный план:
   - 10,000 символов/месяц
   - Достаточно для тестирования

### OpenWeatherMap API Key (опционально)

1. Перейдите на https://openweathermap.org/
2. Зарегистрируйтесь
3. Перейдите в "API Keys"
4. Создайте новый ключ
5. Бесплатный план:
   - 1,000 запросов/день
   - Достаточно для личного использования

---

## Проверка установки

### 1. Проверка Backend

```bash
# Проверка версий
python --version
pip --version

# Проверка зависимостей
pip list | grep fastapi
pip list | grep openai

# Запуск и проверка здоровья
python -m backend.main
# В другом терминале:
curl http://localhost:8000/health
```

### 2. Проверка Frontend

```bash
# Проверка версий
node --version
npm --version

# Проверка зависимостей
cd frontend
npm list react
npm list vite

# Запуск
npm run dev
```

### 3. Тест API

```bash
python test_commands.py
```

### 4. Проверка в браузере

1. Откройте http://localhost:5173
2. Проверьте статус подключения (должен быть зелёный)
3. Разрешите доступ к микрофону
4. Нажмите кнопку микрофона
5. Скажите "Привет Джарвис"
6. Должен прийти голосовой ответ

---

## Решение проблем

### Python не найден

**Windows:**
```cmd
# Переустановите Python с галочкой "Add to PATH"
# Или добавьте вручную в PATH
```

**macOS/Linux:**
```bash
# Используйте python3 вместо python
alias python=python3
```

### pip не работает

```bash
# Windows
python -m pip install --upgrade pip

# macOS/Linux
python3 -m pip install --upgrade pip
```

### Ошибка установки numpy

```bash
# Windows
pip install numpy --only-binary :all:

# macOS (M1/M2)
brew install openblas
pip install numpy

# Linux
sudo apt install python3-numpy
pip install numpy
```

### npm install падает

```bash
# Очистка кэша
npm cache clean --force

# Удаление node_modules
rm -rf node_modules package-lock.json

# Повторная установка
npm install
```

### Порт 8000 занят

```bash
# Найти процесс
# Windows
netstat -ano | findstr :8000

# macOS/Linux
lsof -i :8000

# Изменить порт в .env
PORT=8001
```

### WebSocket не подключается

1. Проверьте что Backend запущен
2. Проверьте `VITE_WS_URL` в `frontend/.env`
3. Проверьте firewall/антивирус
4. Откройте консоль браузера для ошибок

### Микрофон не работает

1. Разрешите доступ в браузере
2. Используйте HTTPS или localhost
3. Проверьте настройки микрофона в системе
4. Попробуйте другой браузер

### API ошибки

**OpenAI:**
- Проверьте ключ в `.env`
- Проверьте баланс аккаунта
- Проверьте лимиты rate limit

**ElevenLabs:**
- Проверьте ключ в `.env`
- Проверьте лимит символов
- Проверьте voice ID

---

## ✅ Контрольный список

- [ ] Python 3.9+ установлен
- [ ] Node.js 16+ установлен
- [ ] Репозиторий клонирован
- [ ] Backend зависимости установлены
- [ ] Frontend зависимости установлены
- [ ] .env файл создан и заполнен
- [ ] OpenAI API ключ получен и работает
- [ ] ElevenLabs API ключ получен и работает
- [ ] Backend запускается без ошибок
- [ ] Frontend запускается без ошибок
- [ ] Браузер открывает http://localhost:5173
- [ ] WebSocket подключается
- [ ] Микрофон работает
- [ ] Команды обрабатываются
- [ ] Голосовые ответы воспроизводятся

---

## 📚 Дополнительные ресурсы

- [README.md](README.md) - Основная документация
- [QUICK_START.md](QUICK_START.md) - Быстрый старт
- [EXAMPLES.md](EXAMPLES.md) - Примеры использования
- [CONTRIBUTING.md](CONTRIBUTING.md) - Как внести вклад

## 💬 Поддержка

Если у вас остались вопросы:
1. Проверьте [Issues на GitHub](https://github.com/yourusername/Jarvis_AI/issues)
2. Создайте новый Issue с деталями проблемы
3. Приложите логи ошибок

---

**Удачной установки! 🚀**
